interface:
  display_name: "Merc Local Ops"
  short_description: "Build, start, and troubleshoot Merc-FJU 3.0 across PowerShell, WSL, and repo-real startup entry points"
  default_prompt: "Use $merc-local-ops to work from the current Merc-FJU 3.0 repo reality: build from src/, use src/startup or src/startup.bash as appropriate, inspect src/merc.ini and generated local ini behavior, check runtime directories such as log/player/mail/debug/vote, and separate local environment issues, tracked runtime file noise, and world-data load failures."
