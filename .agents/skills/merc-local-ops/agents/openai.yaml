interface:
  display_name: "Merc Local Ops"
  short_description: "Build, start, and troubleshoot Merc-FJU 3.0 across PowerShell, WSL, generated merc.ini flows, and repo-real startup entry points"
  default_prompt: "Use $merc-local-ops to work from the current Merc-FJU 3.0 repo reality: build from src/, use src/startup, src/startup.bash, or startup-wsl.ps1 as appropriate, inspect src/merc.sample.ini plus generated src/merc.ini behavior, remember that fixing the sample may require regenerating merc.ini, check runtime directories such as log/player/mail/debug/vote, and separate local environment issues, tracked runtime file noise, stale generated config, and world-data load failures."
